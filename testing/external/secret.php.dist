<?php
/**
 * Copy this file to secret.php (same directory) and replace the value below
 * with a long random string, e.g. from `php -r "echo bin2hex(random_bytes(32));"`.
 *
 * secret.php is required before run.php will accept any request; without it,
 * every request is rejected (fail closed). Do not commit secret.php.
 *
 * Whoever calls run.php?host=...&secret=... must supply this same value.
 */
return 'CHANGE-ME-TO-A-LONG-RANDOM-VALUE';
